# No secrets are required to run this pipeline.
# This placeholder exists so `cp .env.example .env` works out of the box.
PIPELINE_PLACEHOLDER=1

# Optional: only needed if you swap the stand-in diarizer for pyannote.audio,
# whose pretrained models are gated behind a Hugging Face token.
# HF_TOKEN=your_huggingface_token_here
